import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 8) {
            Text("Promptdeck")
            Text("Prompts")
            Text("Commands")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
